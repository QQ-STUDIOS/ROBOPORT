# Development tooling — not needed at runtime.
# Install with:  pip install -r requirements-dev.txt

# Linter / formatter
ruff>=0.15.14

# Type checking
mypy>=2.1.0
types-PyYAML
types-requests
types-jsonschema

# Test runner
pytest>=9.1.1
pytest-cov>=7.1.0

# Supply-chain audit (also run in CI)
pip-audit>=2.10.0
