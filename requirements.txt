# Core runtime
anthropic>=0.45        # Anthropic SDK for the live model loop (scripts/roboport_runtime)
jsonschema>=4.0        # validate.py — schema + registry validation
pyyaml>=6.0            # config/ loaders
